(jbuild_version 1)

;(rule
; ((targets (Lexer.ml))
;  (deps    (Lexer.sedlex.ml))
;  (action  (run ${lib:sedlex:ppx_sedlex} ${<} -o ${@}))))

;; ### Library
(library
 ((name        ocameel)
  (public_name ocameel)
  (libraries
    (core
     gen
     tyre
     sedlex
     compiler-libs.common)
  )
  (inline_tests)
  (preprocess (pps (ppx_expect ppx_inline_test sedlex.ppx ppx_deriving.std)))))
